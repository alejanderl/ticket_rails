<%= form_for @cinema , :html => { :multipart => true } do |f| %>
  <% if @cinema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cinema.errors.count, "error") %> prohibited this cinema from being saved:</h2>

      <ul>
      <% @cinema.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
   <div class="field">
    
    <%= f.file_field :photo %>
    <%= f.file_field :image %>
  </div>
    <% f.fields_for :address do |builder| %>  
  <p>

    <%= builder.label :city, "Question" %><br />  
    <%= builder.text_area :city, :rows => 3 %>  
  </p>  
  <% end %>
  
  <%= f.fields_for :address do |address| %>
  
 
    <%= render :partial => 'shared/address', :locals => {:f => address }%>

  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
