<%= form_for(@cinema) do |f| %>
  <% if @cinema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cinema.errors.count, "error") %> prohibited this cinema from being saved:</h2>

      <ul>
      <% @cinema.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  
  hola1
  <%= @cinema.address %>
  <% f.fields_for(@cinema.address) do |ingredient_form| %>
      holacc
      <p>  
      <%= ingredient_form.label :city %>  
      </p>  
   <% end %>
   caracola
  <% f.fields_for :address do |address| %>
  <%= address.inspect %>
      <p>
      
        
      
      </p>

 
    <%= render :partial => 'shared/address', :locals => {:f => address} %>

  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
